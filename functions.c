#include <stdio.h>

int main()
{
	int a,b,c,i;

	while (i<2) {
	printf("add number of entries:");
	scanf("%i", &a);
	}
	
	c = ((a+b)/2);
	
	c = a%b; //modded a and b
	printf("%i, this is the remainder!",c);
	
	
	return 0;
}

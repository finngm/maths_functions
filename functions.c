#include <stdio.h>

int main()
{
	int a,b,c,i;

	while (i<10) {
	printf("add number of entries:");
	scanf("%i", &a);
	i++;
	}
	
	c = ((a+b)/2);
	
	c = a%b; // a mod b

	return 0;
	
	int i=0;
	while (i<5) {
	printf("add number of entries:");
	scanf("%i", &a);
	i++;
	}
}
